#include <iostream>
#include <string>
#include <cmath>
#include <iomanip>
using namespace std;
int prime(int n)
{
    if (n == 2 || n == 3)
    {
        return 1;
    }
    if (n < 3 || n % 2 == 0 || n % 3 == 0)
    {
        return 0;
    }
    for (int i = 5; i * i <= n; i += 6)
    {
        if (n % i == 0 || n % (i + 2) == 0)
        {
            return 0;
        }
    }
    return 1;
}
int main()
{
    int c;
    cin >> c;
    while (c--)
    {
        int a, b;
        cin >> a >> b;
        if (a > b)
        {
            int tmp = b;
            b = a;
            a = tmp;
        }
        for (int i = a; i <= b; i++)
        {
            if (prime(i))
            {
                cout << i << " ";
            }
        }
        cout << endl;
    }
}
