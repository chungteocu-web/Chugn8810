#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
int main()
{
    int m, n;
    cin >> m;
    while (m--)
    {
        cin >> n;
        vector<int> a(n);
        int x, k, vitri1 = -1;
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
        cin >> k >> x;
        for (int i = 0; i < n; i++)
        {
            if (a[i] == x)
            {
                vitri1 = i;
                break;
            }
        }
        int tmp1 = k / 2;
        for (int i = vitri1 - tmp1; i < vitri1; i++)
        {
            cout << a[i] << " ";
        }
        for (int i = vitri1 + 1; i <= vitri1 + tmp1; i++)
        {
            cout << a[i] << " ";
        }
        cout << endl;
    }
}
