#include <iostream>
#include <string>
#include <cmath>
#include <iomanip>
using namespace std;
int checkfibo(int m)
{
    if (m < 2)
    {
        return 1;
    }
    int fn1 = 1, fn2 = 1, fn = fn1 + fn2;
    while (fn <= m)
    {
        if (fn == m)
        {
            return 1;
        }
        fn = fn1 + fn2;
        fn1 = fn2;
        fn2 = fn;
    }
    return 0;
}
int main()
{
    int m;
    cin >> m;
    while (m--)
    {
        int a;
        cin >> a;
        int A[a];
        for (int i = 0; i < a; i++)
        {
            cin >> A[i];
        }
        for (int i = 0; i < a; i++)
        {
            if (checkfibo(A[i]))
            {
                cout << A[i] << " ";
            }
        }
        cout << endl;
    }
}
